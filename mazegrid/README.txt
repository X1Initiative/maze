Functions to convert a Black-White maze image to an array

run with $python maze.py

Currently, I'm having some issues:
* need to add newline after the width of the list
* output results are by pixel level, which later needs to be adjusted to run bfs
